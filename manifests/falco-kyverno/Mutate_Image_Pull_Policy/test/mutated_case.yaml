apiVersion: v1
kind: Pod
metadata:
  name: test-mutate-latest
  namespace: default
spec:
  containers:
  - name: app
    image: nginx:latest
    resources:
      requests:
        memory: "64Mi"
        cpu: "50m"
      limits:
        memory: "128Mi"
        cpu: "100m"
